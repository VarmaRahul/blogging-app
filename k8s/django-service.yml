apiVersion: v1
kind: Service
metadata:
  name: django-service
  namespace: blog

spec:
  type: NodePort
  selector:
    app: live-django
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30000